import React from 'react';
import { Text, TouchableOpacity, View, StyleSheet } from 'react-native';

import { colors, textStyles, containerStyles } from '../styles';

const FilePicker = ({ props, context }) => {
    return null;
};

export default FilePicker;
